# Grant the bqio service user access to the be quiet! IO-series PSU HID device.
# Install to /etc/udev/rules.d/99-bqio.rules, then:
#   udevadm control --reload && udevadm trigger
SUBSYSTEM=="hidraw", ATTRS{idVendor}=="373f", ATTRS{idProduct}=="0023", TAG+="systemd", MODE="0660", GROUP="bqio"
