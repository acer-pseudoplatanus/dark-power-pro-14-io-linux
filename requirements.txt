# bqio has NO runtime dependencies (zero-dep by design).
# This file pins the DEVELOPMENT toolchain only (quality gates).
#
#   python -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#   .venv/bin/pip install -e .
#
# Production deployment needs nothing but Python >= 3.9:
#   pip install .        # or: pip install /path/to/bqio-1.0.0.tar.gz

pytest>=7
pytest-cov>=4
ruff>=0.4
mypy>=1.8,<2  # mypy 2.x dropped --python-version 3.9 (our floor)
coverage>=7
